import { sayHello, sum } from './src/js/utils.js';

sayHello();

sum(1, 1);
sum(1, 10);

// export files from index.js in libs => import them
// absolute/relative imports (how to set up absolute imports)
